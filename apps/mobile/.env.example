# 기본은 자동 감지 (Expo dev server 호스트 + 아래 PORT) — IP가 자주 바뀌어도 별도 수정 불필요
# 시뮬레이터/에뮬레이터/실기기 모두 같은 LAN의 dev 머신을 자동으로 가리킨다

EXPO_PUBLIC_API_PORT=8000

# 명시 지정이 필요할 때만 주석 해제 (staging/prod, 또는 다른 머신의 API)
# EXPO_PUBLIC_API_BASE_URL=https://api.example.com
