# WebView가 로드할 web-admin URL.
# 기본은 자동 감지 (Expo dev server 호스트 + 아래 PORT) — IP가 자주 바뀌어도 별도 수정 불필요.
# 시뮬레이터/에뮬레이터/실기기 모두 같은 LAN의 dev 머신 web-admin(vite)을 자동으로 가리킨다.
EXPO_PUBLIC_WEB_PORT=3000

# 명시 지정이 필요할 때만 주석 해제 (staging/prod 배포 URL, 또는 다른 머신).
# EXPO_PUBLIC_WEB_URL=https://admin.example.com
