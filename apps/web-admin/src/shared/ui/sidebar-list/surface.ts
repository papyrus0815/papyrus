/**
 * 사이드바 지면색 — 전 지면 공통 **단일 출처**.
 *
 * **회색 판을 두지 않는다.** 사이드바는 본문과 같은 흰 지면이고, 층을 만드는 건 오른쪽
 * 경계선 하나다. (푸른 기 도는 #f4f6fa도, 중립 회색 #f7f7f8도 결국 '목록 뒤에 깔린 판'으로
 * 보였다 — 목록이 지면 위에 얹힌 물건이 아니라 지면 자체여야 한다.)
 *
 * 스타일뿐 아니라 **대비 계산의 기준**이기도 해서(사건 분류 잉크 보정) 스타일 파일에서
 * 떼어 둔다 — styled-components를 끌고 오지 않아야 순수 유틸·테스트가 가볍게 쓴다.
 */
export const SIDEBAR_SURFACE = {
  light: '#ffffff',
  dark: '#151515',
} as const
